**SOS2 Configuration Notes**

	ssh key active on B-link 

	firewall enabled for Apache web server


Documentation of troubleshooting the HTTP server config:

1) apache2's VirtualHost file - granting permissions to edit '.' file:
sudo chown sos /var/www/sos

1a) 

2) Accessing the VirtualHost file from an external computer - configuring the firewall from blocking incoming connections on port 80:
sudo nano /etc/hosts
sudo ss -tlnp | grep :80
