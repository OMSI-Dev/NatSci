Working solution: 



(Step 1: ) tell GDM to use the GTX 1070 specifically

sudo nano /etc/udev/rules.d/61-gdm.rules

```
Add:
```

SUBSYSTEM=="drm", KERNEL=="card2", TAG+="seat", ENV{ID\_SEAT}="seat0"

SUBSYSTEM=="drm", KERNEL=="card1", TAG+="seat", ENV{ID\_SEAT}="seat1"



(Step 2: ) reload udev

sudo udevadm control --reload-rules

sudo udevadm trigger

sudo shutdown -h now

